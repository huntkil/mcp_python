# 📁 수동 설치 가이드 (Git 없이)

Git이 설치되지 않은 환경에서 파일을 직접 복사해서 MCP Python 서버를 설치하는 방법을 안내합니다.

## 📋 목차

- [1. 파일 준비](#1-파일-준비)
- [2. 폴더 구조 생성](#2-폴더-구조-생성)
- [3. 파일 복사](#3-파일-복사)
- [4. 설치 및 설정](#4-설치-및-설정)
- [5. 실행 방법](#5-실행-방법)

---

## 1. 파일 준비

### 🎯 **필요한 파일 목록**

다음 파일들을 USB, 클라우드 드라이브, 또는 네트워크를 통해 복사해야 합니다:

#### 📁 **핵심 소스 코드**

```
src/
├── __init__.py
├── server.py
├── markdown_manager.py
├── obsidian_manager.py
├── utils.py
└── mcp/
    ├── __init__.py
    ├── types.py
    └── server/
        ├── __init__.py
        ├── server.py
        └── stdio.py
```

#### 📁 **설정 및 실행 파일**

```
config.env.example
run_server.sh (macOS/Linux)
run_server.bat (Windows)
install.sh (macOS/Linux)
install.bat (Windows)
requirements.txt
pyproject.toml
```

#### 📁 **문서 파일**

```
사용방법.md
IDE_SETUP.md
RUFF_SETUP.md
README.md
```

#### 📁 **IDE 설정 (선택사항)**

```
.vscode/
├── settings.json
└── settings-windows.json
cursor-settings.json
```

---

## 2. 폴더 구조 생성

### 📂 **대상 컴퓨터에서 폴더 생성**

```bash
# 프로젝트 루트 폴더 생성
mkdir mcp_python
cd mcp_python

# 하위 폴더들 생성
mkdir src
mkdir src/mcp
mkdir src/mcp/server
mkdir tests
mkdir .vscode
```

### 🖥️ **운영체제별 명령어**

#### macOS/Linux

```bash
mkdir -p mcp_python/src/mcp/server
mkdir -p mcp_python/tests
mkdir -p mcp_python/.vscode
cd mcp_python
```

#### Windows

```cmd
mkdir mcp_python
cd mcp_python
mkdir src
mkdir src\mcp
mkdir src\mcp\server
mkdir tests
mkdir .vscode
```

---

## 3. 파일 복사

### 📋 **복사 순서**

#### 1단계: 소스 코드 복사

```
src/ 폴더 전체를 대상 컴퓨터의 mcp_python/src/로 복사
```

#### 2단계: 설정 파일 복사

```
config.env.example → mcp_python/config.env.example
requirements.txt → mcp_python/requirements.txt
pyproject.toml → mcp_python/pyproject.toml
```

#### 3단계: 실행 스크립트 복사

```
# macOS/Linux
run_server.sh → mcp_python/run_server.sh
install.sh → mcp_python/install.sh

# Windows
run_server.bat → mcp_python/run_server.bat
install.bat → mcp_python/install.bat
```

#### 4단계: 문서 파일 복사

```
사용방법.md → mcp_python/사용방법.md
IDE_SETUP.md → mcp_python/IDE_SETUP.md
RUFF_SETUP.md → mcp_python/RUFF_SETUP.md
README.md → mcp_python/README.md
```

#### 5단계: IDE 설정 복사 (선택사항)

```
.vscode/settings.json → mcp_python/.vscode/settings.json
.vscode/settings-windows.json → mcp_python/.vscode/settings-windows.json
cursor-settings.json → mcp_python/cursor-settings.json
```

### 🔄 **복사 방법**

#### USB 드라이브 사용

1. USB 드라이브에 모든 파일 복사
2. 대상 컴퓨터에 USB 연결
3. 파일들을 mcp_python 폴더로 복사

#### 클라우드 드라이브 사용

1. Google Drive, Dropbox, OneDrive 등에 파일 업로드
2. 대상 컴퓨터에서 클라우드 드라이브 접속
3. 파일들을 mcp_python 폴더로 다운로드

#### 네트워크 공유 사용

1. 네트워크 드라이브에 파일 공유
2. 대상 컴퓨터에서 네트워크 드라이브 접속
3. 파일들을 mcp_python 폴더로 복사

---

## 4. 설치 및 설정

### 🐍 **Python 환경 설정**

#### Python 설치 확인

```bash
# macOS/Linux
python3 --version

# Windows
python --version
```

#### 가상환경 생성

```bash
# macOS/Linux
cd mcp_python
python3 -m venv .venv
source .venv/bin/activate

# Windows
cd mcp_python
python -m venv .venv
.venv\Scripts\activate
```

#### 의존성 설치

```bash
# pip 업그레이드
pip install --upgrade pip

# requirements.txt에서 패키지 설치
pip install -r requirements.txt
```

### ⚙️ **설정 파일 생성**

#### config.env 파일 생성

```bash
# config.env.example을 config.env로 복사
cp config.env.example config.env

# 편집기로 config.env 수정
# macOS/Linux
nano config.env
# 또는
code config.env

# Windows
notepad config.env
```

#### config.env 내용 수정

```bash
# 실제 경로로 수정
MARKDOWN_MCP_BASE_PATH=/Users/username/Desktop/mcp_python
OBSIDIAN_VAULT_PATH="/Users/username/Library/Mobile Documents/iCloud~md~obsidian/Documents/My Vault"
```

### 🔐 **실행 권한 설정**

#### macOS/Linux

```bash
chmod +x run_server.sh
chmod +x install.sh
```

---

## 5. 실행 방법

### 🚀 **서버 실행**

#### 자동 설치 스크립트 사용

```bash
# macOS/Linux
./install.sh

# Windows
install.bat
```

#### 수동 실행

```bash
# macOS/Linux
source .venv/bin/activate
./run_server.sh

# Windows
.venv\Scripts\activate
run_server.bat
```

### ✅ **설치 확인**

정상 실행 시 다음 메시지가 출력됩니다:

```
📁 환경 변수 파일 로드 중...
✅ 환경 변수 로드 완료
🔗 Obsidian 볼트 경로: /path/to/vault
📂 기본 경로: /path/to/project
🚀 MCP 서버 시작 중...
INFO - MarkdownManager initialized with base path: ...
INFO - Obsidian manager initialized with vault: ...
INFO - Markdown MCP Server initialized
INFO - Starting Markdown MCP Server...
```

---

## 🔧 **문제 해결**

### ❌ **일반적인 오류**

#### Python이 설치되지 않음

```bash
# macOS (Homebrew)
brew install python3

# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-venv

# Windows
# python.org에서 다운로드
```

#### 가상환경 생성 실패

```bash
# venv 모듈 설치
# Ubuntu/Debian
sudo apt install python3-venv

# 또는 virtualenv 사용
pip install virtualenv
virtualenv .venv
```

#### 패키지 설치 실패

```bash
# pip 업그레이드
pip install --upgrade pip

# 개별 패키지 설치
pip install pyyaml
pip install ruff
```

#### 권한 오류

```bash
# macOS/Linux
chmod +x *.sh
chmod +x *.py

# Windows
# 관리자 권한으로 실행
```

### 🔍 **디버깅**

#### 로그 확인

```bash
# 상세 로그로 실행
python -m src.server --debug
```

#### 환경 변수 확인

```bash
# macOS/Linux
echo $OBSIDIAN_VAULT_PATH
echo $MARKDOWN_MCP_BASE_PATH

# Windows
echo %OBSIDIAN_VAULT_PATH%
echo %MARKDOWN_MCP_BASE_PATH%
```

---

## 📋 **체크리스트**

### ✅ **설치 전 확인사항**

- [ ] Python 3.8+ 설치됨
- [ ] 모든 파일이 올바른 위치에 복사됨
- [ ] 폴더 구조가 정확함

### ✅ **설치 과정**

- [ ] 가상환경 생성 완료
- [ ] 의존성 설치 완료
- [ ] config.env 파일 설정 완료
- [ ] 실행 권한 설정 완료

### ✅ **설치 후 확인**

- [ ] 서버 실행 테스트 성공
- [ ] 로그 메시지 정상 출력
- [ ] IDE 설정 완료 (선택사항)

---

## 📚 **추가 도움말**

### 📖 **관련 문서**

- `사용방법.md`: 상세한 사용 가이드
- `IDE_SETUP.md`: IDE 연동 설정
- `RUFF_SETUP.md`: 코드 품질 관리

### 🆘 **지원**

- 문제 발생 시 로그 메시지 확인
- 설정 파일 경로 재확인
- Python 버전 호환성 확인

---

**이제 Git 없이도 MCP Python 서버를 설치하고 사용할 수 있습니다!** 🚀

모든 파일을 올바른 위치에 복사하고 설정만 완료하면 바로 사용 가능합니다.
