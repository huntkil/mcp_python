# 🚀 MCP Python 서버 사용 가이드

다른 컴퓨터에서 이 프로젝트를 설치하고 사용하는 방법을 상세히 안내합니다.

## 📋 목차

- [1. 프로젝트 개요](#1-프로젝트-개요)
- [2. 시스템 요구사항](#2-시스템-요구사항)
- [3. 설치 방법](#3-설치-방법)
- [4. 설정 방법](#4-설정-방법)
- [5. 사용 방법](#5-사용-방법)
- [6. IDE 연동](#6-ide-연동)
- [7. 문제 해결](#7-문제-해결)
- [8. 고급 기능](#8-고급-기능)

---

## 1. 프로젝트 개요

### 🎯 **이 프로젝트는 무엇인가요?**

**MCP (Model Context Protocol) Python 서버**는 다음과 같은 기능을 제공합니다:

- 📝 **Markdown 문서 관리**: 생성, 읽기, 수정, 삭제, 검색
- 🔗 **Obsidian 볼트 연동**: Obsidian 노트 관리 및 검색
- 🛠️ **IDE 연동**: Cursor, VSCode 등에서 자연어 명령으로 사용
- 📊 **YAML Frontmatter 관리**: 메타데이터 자동 처리
- 🔍 **고급 검색**: 내용 기반 검색 및 태그 관리

### 📁 **프로젝트 구조**
```
mcp_python/
├── src/                    # 소스 코드
│   ├── server.py          # 메인 MCP 서버
│   ├── markdown_manager.py # Markdown 문서 관리
│   ├── obsidian_manager.py # Obsidian 볼트 관리
│   ├── utils.py           # 유틸리티 함수
│   └── mcp/               # MCP 프로토콜 구현
├── tests/                 # 테스트 코드
├── config.env             # 환경 변수 설정
├── run_server.sh          # 서버 실행 스크립트
├── pyproject.toml         # Ruff 린터 설정
├── requirements.txt       # Python 의존성
└── README.md             # 프로젝트 설명
```

---

## 2. 시스템 요구사항

### 💻 **필수 요구사항**
- **OS**: macOS, Linux, Windows
- **Python**: 3.8 이상
- **메모리**: 최소 512MB RAM
- **저장공간**: 최소 100MB

### 🔧 **권장 사항**
- **Python**: 3.9 이상
- **메모리**: 1GB 이상 RAM
- **IDE**: Cursor, VSCode, PyCharm 등

---

## 3. 설치 방법

### 3.1 프로젝트 다운로드

```bash
# Git으로 클론
git clone https://github.com/huntkil/mcp_python.git
cd mcp_python

# 또는 ZIP 파일 다운로드 후 압축 해제
```

### 3.2 Python 환경 설정

#### macOS/Linux
```bash
# Python 3.8+ 설치 확인
python3 --version

# 가상환경 생성
python3 -m venv .venv

# 가상환경 활성화
source .venv/bin/activate

# 의존성 설치
pip install -r requirements.txt
```

#### Windows
```bash
# Python 3.8+ 설치 확인
python --version

# 가상환경 생성
python -m venv .venv

# 가상환경 활성화
.venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt
```

### 3.3 설치 확인

```bash
# 서버 실행 테스트
python -m src.server

# 정상 실행 시 아래 메시지 출력:
# INFO - MarkdownManager initialized with base path: ...
# INFO - Markdown MCP Server initialized
# INFO - Starting Markdown MCP Server...
```

---

## 4. 설정 방법

### 4.1 환경 변수 설정

`config.env` 파일을 편집하여 프로젝트 경로와 Obsidian 볼트 경로를 설정합니다:

```bash
# config.env
MARKDOWN_MCP_BASE_PATH=/path/to/your/project
OBSIDIAN_VAULT_PATH="/path/to/your/obsidian/vault"
```

#### macOS 예시
```bash
MARKDOWN_MCP_BASE_PATH=/Users/username/Desktop/mcp_python
OBSIDIAN_VAULT_PATH="/Users/username/Library/Mobile Documents/iCloud~md~obsidian/Documents/My Vault"
```

#### Windows 예시
```bash
MARKDOWN_MCP_BASE_PATH=C:\Users\username\Desktop\mcp_python
OBSIDIAN_VAULT_PATH="C:\Users\username\Documents\Obsidian\My Vault"
```

### 4.2 실행 스크립트 설정

`run_server.sh` (macOS/Linux) 또는 `run_server.bat` (Windows) 파일의 경로를 수정합니다.

#### macOS/Linux
```bash
# run_server.sh 실행 권한 부여
chmod +x run_server.sh

# 경로 확인 및 수정
./run_server.sh
```

#### Windows
```cmd
# run_server.bat 실행
run_server.bat
```

---

## 5. 사용 방법

### 5.1 기본 사용법

#### 서버 실행
```bash
# 방법 1: 실행 스크립트 사용
./run_server.sh

# 방법 2: 직접 실행
source .venv/bin/activate
python -m src.server

# 방법 3: 환경 변수와 함께 실행
OBSIDIAN_VAULT_PATH="/path/to/vault" python -m src.server
```

#### 서버 중지
```bash
# 터미널에서 Ctrl+C
```

### 5.2 명령어 예시

#### Markdown 문서 관리
```bash
# 새 문서 생성
echo "새로운 프로젝트 문서를 생성해주세요"

# 문서 읽기
echo "README.md 파일을 읽어줘"

# 문서 검색
echo "API 키워드가 포함된 문서를 찾아줘"

# 문서 수정
echo "README.md에 설치 가이드를 추가해줘"
```

#### Obsidian 볼트 관리
```bash
# 볼트 정보 확인
echo "Obsidian 볼트 정보를 보여줘"

# 노트 목록 확인
echo "My Card 볼트의 모든 노트를 보여줘"

# 노트 검색
echo "AI 관련 노트를 찾아줘"

# 새 노트 생성
echo "새로운 아이디어 노트를 생성해줘"
```

---

## 6. IDE 연동

### 6.1 Cursor IDE 설정

`cursor-settings.json` 파일을 프로젝트 루트에 생성:

```json
{
  "mcp": {
    "servers": {
      "markdown-manager": {
        "command": "bash",
        "args": ["-c", "source .venv/bin/activate && python -m src.server"],
        "cwd": "/path/to/your/mcp_python"
      }
    }
  }
}
```

#### macOS/Linux
```json
{
  "mcp": {
    "servers": {
      "markdown-manager": {
        "command": "bash",
        "args": ["-c", "source .venv/bin/activate && python -m src.server"],
        "cwd": "/Users/username/Desktop/mcp_python"
      }
    }
  }
}
```

#### Windows
```json
{
  "mcp": {
    "servers": {
      "markdown-manager": {
        "command": "cmd",
        "args": ["/c", ".venv\\Scripts\\activate && python -m src.server"],
        "cwd": "C:\\Users\\username\\Desktop\\mcp_python"
      }
    }
  }
}
```

### 6.2 VSCode 설정

`.vscode/settings.json` 파일 생성:

```json
{
  "mcp.servers": {
    "markdown-manager": {
      "command": "bash",
      "args": ["-c", "source .venv/bin/activate && python -m src.server"],
      "cwd": "/path/to/your/mcp_python"
    }
  },
  "python.linting.enabled": true,
  "python.linting.ruffEnabled": true,
  "python.formatting.provider": "ruff"
}
```

### 6.3 IDE 재시작

설정 변경 후 IDE를 재시작하여 MCP 서버를 활성화합니다.

---

## 7. 문제 해결

### 7.1 일반적인 오류

#### ModuleNotFoundError: No module named 'mcp'
```bash
# 해결: 가상환경이 활성화되지 않음
source .venv/bin/activate  # macOS/Linux
.venv\Scripts\activate     # Windows
```

#### ModuleNotFoundError: No module named 'yaml'
```bash
# 해결: PyYAML 설치
pip install pyyaml
```

#### Permission denied
```bash
# 해결: 실행 권한 부여
chmod +x run_server.sh
```

#### Obsidian 볼트 경로 오류
```bash
# 해결: config.env에서 경로 확인
cat config.env
# 올바른 경로로 수정
```

### 7.2 디버깅 방법

#### 로그 확인
```bash
# 상세 로그로 실행
python -m src.server --debug
```

#### 환경 변수 확인
```bash
# 현재 환경 변수 확인
echo $OBSIDIAN_VAULT_PATH
echo $MARKDOWN_MCP_BASE_PATH
```

#### 파일 권한 확인
```bash
# 파일 권한 확인
ls -la run_server.sh
ls -la config.env
```

### 7.3 성능 최적화

#### 메모리 사용량 최적화
```bash
# 가상환경에서만 실행
source .venv/bin/activate
python -m src.server
```

#### 대용량 볼트 처리
```bash
# 설정에서 검색 범위 제한
# obsidian_manager.py에서 검색 옵션 조정
```

---

## 8. 고급 기능

### 8.1 코드 품질 관리

#### Ruff 린터 사용
```bash
# 린트 검사
ruff check src/

# 자동 수정
ruff check --fix src/

# 코드 포맷팅
ruff format src/
```

#### 테스트 실행
```bash
# 전체 테스트
python -m pytest tests/

# 특정 테스트
python -m pytest tests/test_markdown_manager.py
```

### 8.2 커스터마이징

#### 새로운 도구 추가
1. `src/server.py`의 `setup_tools()` 함수에 새 도구 추가
2. `handle_call_tool()` 함수에 처리 로직 추가
3. 테스트 코드 작성

#### Obsidian 플러그인 연동
1. Obsidian API 플러그인 설치
2. `obsidian_manager.py`에 API 연동 기능 추가
3. 실시간 동기화 설정

### 8.3 배포 및 배포

#### Docker 컨테이너화
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "-m", "src.server"]
```

#### 시스템 서비스 등록
```bash
# systemd 서비스 파일 생성
sudo nano /etc/systemd/system/mcp-server.service
```

---

## 📚 추가 리소스

### 🔗 **관련 링크**
- [MCP 공식 문서](https://modelcontextprotocol.io/)
- [Cursor IDE MCP 가이드](https://cursor.sh/docs/mcp)
- [Ruff 린터 문서](https://docs.astral.sh/ruff/)
- [Obsidian API 문서](https://github.com/obsidianmd/obsidian-api)

### 📖 **추가 문서**
- `IDE_SETUP.md`: IDE별 상세 설정 가이드
- `RUFF_SETUP.md`: 코드 품질 관리 가이드
- `서버실행.md`: 서버 실행 상세 가이드

### 🆘 **지원 및 문의**
- GitHub Issues: [프로젝트 이슈 페이지](https://github.com/huntkil/mcp_python/issues)
- 문서 업데이트: PR(Pull Request) 환영

---

## 🎯 **빠른 시작 체크리스트**

- [ ] Python 3.8+ 설치 확인
- [ ] 프로젝트 클론/다운로드
- [ ] 가상환경 생성 및 활성화
- [ ] 의존성 설치 (`pip install -r requirements.txt`)
- [ ] `config.env` 파일 설정
- [ ] 서버 실행 테스트
- [ ] IDE 설정 (선택사항)
- [ ] 첫 번째 명령 실행

---

**이제 다른 컴퓨터에서도 MCP Python 서버를 쉽게 사용할 수 있습니다!** 🚀

문제가 발생하거나 추가 도움이 필요하면 언제든 문의해 주세요. 